"""pytest configuration for the get_started_claude_sdk_client package.

The ``util_claude`` package path is configured via ``pythonpath`` in
``pytest.ini`` (equivalent to setting PYTHONPATH before invoking pytest),
so no sys.path manipulation is needed here.
"""
